services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: galaxy
      POSTGRES_USER: galaxy
      POSTGRES_PASSWORD: galaxy
    ports:
      - "5432:5432"
    volumes:
      - galaxy-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U galaxy -d galaxy"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  galaxy-postgres-data:
